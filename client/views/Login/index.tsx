import React from 'react';

import Modal from './components/Modal';

const Login: React.FC = ({}) => {
    return (
        <Modal>
            <h1>Login</h1>
        </Modal>
    );
};

export default Login;